name: romeu_lanches_mobile
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  hive_flutter: ^1.1.0
  google_fonts: ^8.1.0
  signals_flutter: ^7.1.0
  cupertino_icons: ^1.0.8
  intl: ^0.20.3
  http: ^1.2.2
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets: 
    - assets/images/